/* const markHeight = 1.8;
const markMass = 83;

const johnHeight = 1.75;
const johnMass = 79; */

//////////
/* const markBMI = markMass / (markHeight * markHeight);
console.log(markBMI);
const johnBMI = johnMass / (johnHeight * johnHeight);
console.log(johnBMI);

const markHigherBMI = markBMI > johnBMI;
console.log(markHigherBMI);

// coding challenge #2

console.log(`John's BMI is higher than Mark's!`);

if (markBMI < johnBMI) {
  console.log(`John's BMI is higher than Mark's!`);
} else {
  console.log(`John's is no longer higher than Mark's!`);
} */

// coding challenge #3

// const dolphinsAve = (96 + 108 + 89) / 3;
// const koalasAve = (88 + 91 + 110) / 3;

// if (dolphinsAve > koalasAve) {
//   console.log(`The dolphins are the winners!`);
// } else if (dolphinsAve === koalasAve) {
//   console.log(`We have a tie!`);
// } else if (dolphinsAve < koalasAve) {
//   console.log(`The Koalas are the winners!`);
// }

// Bonus 1
/* const dolphinsAve = (96 + 108 + 89) / 3;
const koalasAve = (88 + 91 + 110) / 3;

if (dolphinsAve > koalasAve && dolphinsAve >= 100) {
  console.log(`The dolphins are the winners!`);
} else if (
  dolphinsAve === koalasAve &&
  dolphinsAve >= 100 &&
  koalasAve >= 100
) {
  console.log(`We have a tie! Both teams are winners!!`);
} else if (dolphinsAve < koalasAve && koalasAve >= 100) {
  console.log(`The Koalas are the winners!`);
} else {
  console.log(`Both teams are losers!!`);
} */

// Codingchallenge #4

//test values are 275, 40 and 430

/* let bill = 40;
let tip = bill >= 50 && bill <= 300 ? (bill / 100) * 15 : (bill / 100) * 20;
let totalDue = bill + tip;

console.log(`Your bill is ${bill}`);
console.log(`Your tip is ${tip}`);
console.log(`Your total amount payable is ${totalDue}`); */
